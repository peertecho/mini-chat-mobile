// This file is autogenerated by the hyperdispatch compiler
/* eslint-disable camelcase */

const { c, b4a, assert } = require('hyperdispatch/runtime')
const { version, getEncoding, setVersion } = require('./messages.js')

const defaultVersion = version

class Router {
  constructor () {
    this._handler0 = null
    this._handler1 = null
    this._handler2 = null
    this._handler3 = null
    this._handler4 = null
    this._handler5 = null
    this._handler6 = null
    this._handler7 = null
    this._handler8 = null

    this._missing = 9
  }

  add (name, handler) {
    switch (name) {
      case '@minichat/add-writer':
        this._handler0 = handler
        break
      case '@minichat/add-invite':
        this._handler1 = handler
        break
      case '@minichat/del-invite':
        this._handler2 = handler
        break
      case '@minichat/add-user':
        this._handler3 = handler
        break
      case '@minichat/del-user':
        this._handler4 = handler
        break
      case '@minichat/add-message':
        this._handler5 = handler
        break
      case '@minichat/del-message':
        this._handler6 = handler
        break
      case '@minichat/add-event':
        this._handler7 = handler
        break
      case '@minichat/del-event':
        this._handler8 = handler
        break
      default:
        throw new Error('Cannot register a handler for a nonexistent route: ' + name)
    }
    this._missing--
  }

  _checkAll () {
    assert(this._handler0 !== null, 'Missing handler for "@minichat/add-writer"')
    assert(this._handler1 !== null, 'Missing handler for "@minichat/add-invite"')
    assert(this._handler2 !== null, 'Missing handler for "@minichat/del-invite"')
    assert(this._handler3 !== null, 'Missing handler for "@minichat/add-user"')
    assert(this._handler4 !== null, 'Missing handler for "@minichat/del-user"')
    assert(this._handler5 !== null, 'Missing handler for "@minichat/add-message"')
    assert(this._handler6 !== null, 'Missing handler for "@minichat/del-message"')
    assert(this._handler7 !== null, 'Missing handler for "@minichat/add-event"')
    assert(this._handler8 !== null, 'Missing handler for "@minichat/del-event"')
  }

  async dispatch (message, context) {
    if (this._missing > 0) {
      this._checkAll()
    }

    setVersion(defaultVersion)

    const op = b4a.isBuffer(message) ? decode(message) : message

    switch (op.id) {
      case 0:
        return this._handler0(op.value, context)
      case 1:
        return this._handler1(op.value, context)
      case 2:
        return this._handler2(op.value, context)
      case 3:
        return this._handler3(op.value, context)
      case 4:
        return this._handler4(op.value, context)
      case 5:
        return this._handler5(op.value, context)
      case 6:
        return this._handler6(op.value, context)
      case 7:
        return this._handler7(op.value, context)
      case 8:
        return this._handler8(op.value, context)
      default:
        throw new Error('Handler not found for ID:' + op.id)
    }
  }
}

function encode (name, message, { version = defaultVersion } = {}) {
  const state = { buffer: null, start: 0, end: 0 }

  const route = getRouteByName(name)
  setVersion(version)

  c.uint.preencode(state, route.id)
  route.enc.preencode(state, message)

  state.buffer = b4a.allocUnsafe(state.end)
  c.uint.encode(state, route.id)
  route.enc.encode(state, message)

  return state.buffer
}

function decode (buffer, { version = defaultVersion } = {}) {
  const state = { buffer, start: 0, end: buffer.length }

  const id = c.uint.decode(state)
  const route = getRouteById(id)
  setVersion(version)

  const value = route.enc.decode(state)
  return { id, name: route.name, value }
}

const route0 = {
  name: '@minichat/add-writer',
  id: 0,
  enc: getEncoding('@minichat/writers')
}

const route1 = {
  name: '@minichat/add-invite',
  id: 1,
  enc: getEncoding('@minichat/invites')
}

const route2 = {
  name: '@minichat/del-invite',
  id: 2,
  enc: getEncoding('@minichat/invites')
}

const route3 = {
  name: '@minichat/add-user',
  id: 3,
  enc: getEncoding('@minichat/users')
}

const route4 = {
  name: '@minichat/del-user',
  id: 4,
  enc: getEncoding('@minichat/users')
}

const route5 = {
  name: '@minichat/add-message',
  id: 5,
  enc: getEncoding('@minichat/messages')
}

const route6 = {
  name: '@minichat/del-message',
  id: 6,
  enc: getEncoding('@minichat/messages')
}

const route7 = {
  name: '@minichat/add-event',
  id: 7,
  enc: getEncoding('@minichat/events')
}

const route8 = {
  name: '@minichat/del-event',
  id: 8,
  enc: getEncoding('@minichat/events')
}

function getRouteByName (name) {
  switch (name) {
    case '@minichat/add-writer':
      return route0
    case '@minichat/add-invite':
      return route1
    case '@minichat/del-invite':
      return route2
    case '@minichat/add-user':
      return route3
    case '@minichat/del-user':
      return route4
    case '@minichat/add-message':
      return route5
    case '@minichat/del-message':
      return route6
    case '@minichat/add-event':
      return route7
    case '@minichat/del-event':
      return route8
    default:
      throw new Error('Handler not found for name: ' + name)
  }
}

function getRouteById (id) {
  switch (id) {
    case 0:
      return route0
    case 1:
      return route1
    case 2:
      return route2
    case 3:
      return route3
    case 4:
      return route4
    case 5:
      return route5
    case 6:
      return route6
    case 7:
      return route7
    case 8:
      return route8
    default:
      throw new Error('Handler not found for ID: ' + id)
  }
}

module.exports = {
  version,
  encode,
  decode,
  Router
}
